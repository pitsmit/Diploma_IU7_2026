\chapter{Аналитический раздел}

В данном разделе проводится анализ существующих решений,
формализуются требования к разрабатываемому комплексу. Также приводится описание информации, 
используемой программным комплексом в процессе работы и анализируются способы её хранения.

\section{Сравнительный анализ существующих решений}

Существующие программные средства защиты от утечки и утери данных условно можно разделить на две основные категории: 
корпоративные \texttt{DLP (Data Leak Prevention)}-системы и решения, предназначенные для персонального использования. 
Корпоративные системы ориентированы на эксплуатацию в инфраструктуре организаций и, как правило, имеют распределённую 
архитектуру, включающую серверные компоненты, средства централизованного администрирования, мониторинга и 
управления большим количеством рабочих станций. Такие решения обладают широким набором функций контроля и аудита, 
однако их использование на отдельном персональном компьютере зачастую является избыточным как с функциональной, 
так и с ресурсной точки зрения. В рамках данного анализа рассматриваются только решения, предназначенные для личного использования. 

Для анализа были выбраны следующие программные комплексы:

\begin{enumerate}
    \item \texttt{USBGuard}~\cite{USBGuard};
    \item \texttt{USBFilter}~\cite{USBFilter};
    \item \texttt{USBWall}~\cite{USBWall};
    \item \texttt{USBShield}~\cite{USBShield};
    \item \texttt{Ratool}~\cite{Ratool};
    \item \texttt{GiliSoft USB Lock}~\cite{GilisoftUSBLock};
    \item \texttt{USB Block}~\cite{USBBlock}.
\end{enumerate}

В таблице~\ref{tab:compare_solutions} представлены результаты анализа существующих решений.

\begin{table}[H]
    \caption{Результат анализа существующих решений}
    \begin{tabular}{|p{10cm}|c|c|c|c|c|c|c|}
    \hline
    \diagbox[width=10.5cm]{\textbf{Критерий}}{\textbf{Номер решения}} & \textbf{1} & \textbf{2} & \textbf{3} & \textbf{4} & \textbf{5} & \textbf{6} & \textbf{7}\\
    \hline
    поддержка ОС (операционной системы) Linux Ubuntu & + & + & + & - & - & - & - \\ \hline
    возможность создания списка доверенных устройств & + & + & + & + & - & + & + \\ \hline
    возможность установки даты истечения периода доверия к устройству & - & - & - & - & - & - & - \\ \hline
    наличие графического интерфейса & - & - & - & + & + & + & + \\ \hline
    наличие обновлений и поддержки & + & - & - & + & + & + & + \\ \hline
    \end{tabular}
    \label{tab:compare_solutions}
\end{table}

Таким образом, представленные решения не соответствуют в полной мере поставленным критериям.

\section{Диаграмма прецедентов разрабатываемого комплекса}

На рисунке~\ref{img:use_cases} представлена диаграмма прецедентов разрабатываемого комплекса.

\includeimage
    {use_cases}
    {f}
    {H}
    {0.70\textwidth}
    {Диаграмма прецедентов комплекса}

\section{Монтирование устройств в Linux}

Монтирование является связующим звеном между отдельными файловыми системами устройств и единой иерархией каталогов.

\subsection{Иерархия каталогов}

В Linux файлы организованы в виде древовидной структуры (дерева), называемой иерархией 
каталогов~\cite{BaseTheory}. Каждый файл имеет имя, определяющее его расположение в дереве 
каталогов. Корнем этого дерева является корневой каталог (root directory), имеющий имя <</>>. 
Каждый файл или каталог имеет уникальное имя, определяющее его расположение в дереве, 
а именно путь --- список каталогов, которые необходимо пройти, чтобы достичь нужного объекта.

\subsection{Понятие монтирования}

Монтирование --- это механизм подключения файловой системы к единой корневой иерархии каталогов~\cite{TannenBaum}. 
Монтирование связывает файловую систему с указанным каталогом, называемым точкой монтирования. 
После этого все файлы подключённой системы становятся доступны через путь, начинающийся от точки монтирования. 
Если точка монтирования содержит собственные файлы, они становятся недоступны на время подключения, поэтому на 
практике используются пустые каталоги.

\subsection{Специальные файлы}

Устройства ввода-вывода в Linux представлены специальными файлами~\cite{TannenBaum}. При
этом с ними можно проводить операции чтения и записи, используя те же системные вызовы, которые 
применяются для чтения и записи файлов. Существуют два вида специальных файлов: блочные 
специальные файлы и символьные специальные файлы. Блочные специальные файлы используются для 
моделирования устройств, содержащих набор блоков с произвольной адресацией, таких как диски. 
Аналогичным образом символьные специальные файлы используются для моделирования принтеров, 
модемов и других устройств, которые принимают или выдают поток символов. В соответствии с принятым 
соглашением специальные файлы хранятся в каталоге \texttt{/dev}~\cite{FHS}. 

\subsection{Функции управления монтированием}

Монтирование и размонтирование файловой системы осуществляется с помощью функций \texttt{mount}~\cite{Mount} и 
\texttt{umount}~\cite{Umount} соответственно. Функции объявлены в файле \texttt{sys/mount.h}.

\subsubsection{Функция mount}

Заголовок функции \texttt{mount()} представлен в листинге~\ref{lst:mount.cpp}: 

\includelisting
    {mount.cpp}
    {Заголовок \texttt{mount()}}

где:

\begin{itemize}[label=---]
    \item \texttt{source} --- специальный файл устройства;
    \item \texttt{target} --- имя точки монтирования;
    \item \texttt{filesystemtype} --- имя файловой системы устройства. Доступные ядру системы перечислены в файле \texttt{/proc/filesystems};
    \item \texttt{mountflags} --- флаги монтирования;
    \item \texttt{data} --- строка параметров, перечисленных через запятую. Названия и значения параметров 
    индивидуальны для каждой файловой системы.
\end{itemize}

Флаги монтирования используются для изменения поведения \texttt{mount()}.
К основным флагам монтирования относятся:

\begin{itemize}[label=---]
    \item \texttt{MS\_MOVE} --- изменение точки монтирования. При указании этого флага в \texttt{source}
    содержится не путь к файлу устройства, а существующая точка монтирования;
    \item \texttt{MS\_RDONLY} --- монтирует файловую систему в режиме <<Только для чтения>>;
    \item \texttt{MS\_NOEXEC} --- запрещает выполнение программ в этой файловой системе;
    \item \texttt{MS\_REMOUNT} --- перемонтирование существующего монтирования. Позволяет изменить 
    \texttt{mountflags} и \texttt{data} существующих монтирований без необходимости размонтировать и заново 
    монтировать файловую систему.
\end{itemize}

\subsubsection{Функция umount}

Заголовок функции \texttt{umount()} представлен в листинге~\ref{lst:umount.cpp}: 

\includelisting
    {umount.cpp}
    {Заголовок \texttt{umount()}}

где \texttt{target} --- имя точки монтирования.

Существует также функция \texttt{umount2()}, который, как и \texttt{umount()}, размонтирует заданный объект, 
но позволяет указать дополнительные флаги, контролирующие поведение операции. Заголовок функции 
\texttt{umount2()} представлен в листинге~\ref{lst:umount2.cpp}: 

\includelisting
    {umount2.cpp}
    {Заголовок \texttt{umount2()}}

Флаг \texttt{MNT\_FORCE} --- немедленное размонтирование, даже если в данный момент производятся операции чтения/записи над файлами
данной файловой системы.

Флаг \texttt{MNT\_DETACH} --- точка монтирования становится недоступной для новых операций, однако файловая система остаётся 
примонтированной для процессов, которые уже используют её файлы.

\section{Формализация и описание информации, подлежащей хранению разрабатываемым комплексом}

Формальное описание устройства может быть представлено следующими аттрибутами:

\begin{enumerate}
    \item идентификатор производителя;
    \item идентификатор устройства;
    \item уникальный серийный номер устройства;
    \item наименование производителя;
    \item наименование устройства.
\end{enumerate}

Доверенное устройство характеризуется формальным описанием устройства и датой истечения доверия.

Запись о монтировании устройства описывается следующей информацией:

\begin{enumerate}
    \item формальное описание устройства;
    \item узел устройства;
    \item путь к папке монтирования;
    \item режим монтирования (<<Только чтение>> или <<Чтение и запись>>).
\end{enumerate}

\section{Анализ подходов к организации хранилища информации}

Наиболее распространёнными подходами к организации хранилища данных, используемых
программным комплексом, являются использование баз данных и хранение информации
в плоских файлах. 

В таблице~\ref{tab:compare_storages} представлены результаты сравнительного анализа 
способов организации хранилищ данных.

\begin{table}[H]
    \caption{Результат анализа подходов к хранению информации}
    \begin{tabular}{|p{11cm}|p{2cm}|p{2cm}|}
    \hline
    \diagbox[width=11.4cm, height=2.2cm]
    {\textbf{Критерий}}
    {\textbf{Решение}}
    & \textbf{Плоские файлы}
    & \textbf{Базы данных} \\
    \hline

    наличие встроенных механизмов поддержания целостности данных 
    (ограничения, уникальность) 
    & нет & да \\ \hline

    устойчивость хранилища к сбоям и некорректному завершению работы 
    & нет & да \\ \hline

    отсутствие дополнительных зависимостей у прикладного приложения 
    & да & нет \\ \hline

    отсутствие возможности несанкционированного ручного просмотра 
    и редактирования данных без специализированных инструментов 
    & нет & да \\ \hline

    возможность выполнения выборок и фильтрации данных 
    без дополнительной реализации алгоритмов поиска 
    & нет & да \\ \hline

    отсутствие необходимости инициализации и настройки хранилища 
    перед началом работы 
    & да & нет \\ \hline

    возможность расширения структуры данных и добавления новых требований 
    без переработки существующей логики хранения 
    & нет & да \\ \hline

    \end{tabular}
    \label{tab:compare_storages}
\end{table}

На основе таблицы анализа для разрабатываемого комплекса предпочтительнее использовать базу данных.

\section{Анализ существующих баз данных на основе формализации данных}

Базы данных подразделяются на три основных типа по модели данных:

\begin{enumerate}
    \item дореляционные;
    \item реляционные;
    \item постреляционные.
\end{enumerate}

\subsection{Дореляционные базы данных}
Дореляционные базы данных~\cite{DBTypes} хранят записи в виде связей предок/потомок. 
Основными недостатками дореляционных баз данных являются избыточность данных и 
сложность выполнения запросов. Изменение схемы базы данных крайне ограничена. 
Целостность данных обеспечивается за счёт жёсткой структуры связей. Пример хранения записей
в дореляционных базах представлен на рисунке~\ref{img:before_relation_db.drawio}.

\includeimage
    {before_relation_db.drawio}
    {f}
    {H}
    {0.70\textwidth}
    {Пример хранения записей в дореляционных базах данных}

\subsection{Реляционные базы данных}
В реляционных базах данных данные хранятся в виде двумерных таблиц.
Связь между ними обеспечивается общими столбцами, такими как идентификаторы. 
Табличная архитектура подходит для хранения структурированной информации.
Целостность и непротиворечивость данных соблюдается при помощи ограничений. Пример хранения записей
в реляционных базах представлен на рисунке~\ref{img:relation_db.drawio}.

\includeimage
    {relation_db.drawio}
    {f}
    {H}
    {0.70\textwidth}
    {Пример хранения записей в реляционных базах данных}

\subsection{Постреляционные базы данных}
Постреляционные базы данных подходят для хранения неструктурированных
данных, поддерживая логические связи между ними. Недостатком является сложность 
обеспечения целостности данных. Отличаются хорошей горизонтальной
масштабируемостью, сложность запросов варьируется в зависимости от конкретной реализации.

\subsection{Вывод}
Для реализации поставленной задачи была выбрана реляционная модель, так как она лучше всего 
подходит для хранения структурированной информации, обеспечивая баланс между целостностью данных, 
простотой запросов и возможностями масштабирования.

\section{Анализ существующих типов систем управления базами данных (СУБД) по способу доступа}

СУБД по способу доступа к базе данных подразделяется на три основных вида~\cite{SUDBTypes}:

\begin{enumerate}
    \item файл-серверные;
    \item клиент-серверные;
    \item встраиваемые.
\end{enumerate}

\subsection{Файл-серверные СУБД}

При работе с файл-серверной СУБД обработка данных происходит на
рабочем месте, а сервер применяться только как раздельный накопитель. 
Каждый пользователь сам использует информацию и вносит изменения в файлы
данных и в индексные файлы. На рисунке~\ref{img:file_server_subd.drawio}
представлена общая схема работы файл-серверных СУБД.

\includeimage
    {file_server_subd.drawio}
    {f}
    {H}
    {0.70\textwidth}
    {Схема работы файл-серверных СУБД}

\subsection{Клиент-серверные СУБД}

Клиент-серверная СУБД позволяет обмениваться клиенту и серверу минимально необходимыми объёмами информации. 
Клиент может выполнять функции предварительной обработки перед передачей информации серверу, но в основном 
его функции заключаются в организации доступа пользователя к серверу. В большинстве случаев клиент-серверная 
СУБД гораздо менее требовательна к пропускной способности компьютерной сети, чем файл-серверная СУБД, особенно 
при выполнении операции поиска в базе данных по заданным пользователем параметрам, т.к. для поиска нет необходимости 
получать на клиент весь массив данных: клиент передаёт параметры запроса серверу, а сервер производит поиск 
по полученному запросу в локальной базе данных. Результат выполнения запроса возвращается клиенту, 
который обеспечивает отображение результата пользователю. На рисунке~\ref{img:client_server_subd.drawio}
представлена общая схема работы клиент-серверных СУБД.

\includeimage
    {client_server_subd.drawio}
    {f}
    {H}
    {1\textwidth}
    {Схема работы клиент-серверных СУБД}

\subsection{Встраиваемые СУБД}

Встраиваемая система управления базой данных --- это система, которая может быть связана с 
клиентским приложением таким образом, чтобы приложение и СУБД работали в едином адресном пространстве. 
Вместе со встроенной базой данных приложение может быть развернуто как единая программа. Благодаря связыванию 
приложения с базой данных, прикладная система выигрывает от снижения общей сложности и уменьшения затрат на администрирование.
На рисунке~\ref{img:embedded_subd.drawio} представлена общая схема работы встраиваемых СУБД.

\includeimage
    {embedded_subd.drawio}
    {f}
    {H}
    {1\textwidth}
    {Схема работы встраиваемых СУБД}

\subsection{Сравнительный анализ рассмотренных СУБД}

В таблице~\ref{tab:compare_sudb} представлены результаты сравнения рассмотренных СУБД.

\begin{table}[H]
    \caption{Результаты сравнения СУБД по способу доступа}
    \centering
    \begin{tabular}{|p{6.5cm}|p{2.5cm}|p{2.5cm}|p{3.5cm}|}
    \hline

    \rule{0pt}{1.6em}
    \diagbox[width=6.8cm, height=2cm]
    {\textbf{Критерий}}
    {\textbf{Тип СУБД}}

    & \textbf{Файл-серверная}
    & \textbf{Клиент-серверная}
    & \textbf{Встраиваемая} \\
    \hline

    Отсутствие избыточности для локального однопользовательского приложения
    & да & нет & да \\ \hline

    Отсутствие необходимости сетевого взаимодействия
    & нет & нет & да \\ \hline

    Отсутствие необходимости в отдельном серверном процессе
    & да & нет & да \\ \hline

    \end{tabular}
    \label{tab:compare_sudb}
\end{table}

Таким образом, на основании сравнения СУБД, для разрабатываемого программно-алгоритмического комплекса
наиболее подходящей будет встраиваемая СУБД.

\section{Выводы}

В данном разделе была проанализирована предметная область рисков использования USB-устройств, проведено 
сравнение существующих решений по сформулированным критериям. Программно-алгоритмический комплекс был 
формализован с использованием диаграммы прецедентов, описаны представления ключевых структур данных, 
проанализированы способы организации хранилища, выбраны наиболее подходящие типы базы данных и СУБД.